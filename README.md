##
این پروژه یک **پلتفرم جستجو و مرورگر اطلاعات انیمه** است که برای ارائه سریع‌ترین و روان‌ترین تجربه ممکن طراحی شده است. هسته اصلی این ابزار، استفاده از **API هوش مصنوعی Google Gemini** برای بومی‌سازی، تحلیل و تولید محتوای مرتبط است.

-----

### 🤖 معماری و تکنولوژی‌ها

این پروژه به صورت **Serverless** و با استفاده از توابع Netlify (Netlify Functions) برای برقراری ارتباط امن با سرویس‌های خارجی پیاده‌سازی شده است.

  * **Frontend (سمت کاربر):** HTML، CSS (با استفاده از **Tailwind CSS** برای استایل‌دهی مدرن)، و JavaScript خالص (ES Modules).
  * **Backend (توابع Netlify):** JavaScript/Node.js برای پیاده‌سازی منطق سمت سرور (Proxy و پردازش هوش مصنوعی).
  * **دیتابیس/کش:** **Upstash Redis** برای ذخیره‌سازی موقت نتایج جستجو و ترجمه‌ها جهت افزایش سرعت و کاهش هزینه‌های API.

-----

### 🔗 سرویس‌های خارجی مورد استفاده

1.  **Jikan API (API جایگزین MyAnimeList):**

      * برای دریافت اطلاعات خام انیمه، جزئیات شخصیت‌ها، ویدیوها، و لیست‌های رتبه‌بندی (تاپ، در حال پخش و غیره) استفاده می‌شود.
      * **نحوه دسترسی:** ارتباط از طریق فایل `jikan-proxy.js` برقرار می‌شود.

2.  **Google Gemini API (مدل `gemini-2.5-flash-preview-09-2025`):**

      * وظیفه اصلی آن **ترجمه مفهومی** عناوین، خلاصه داستان‌ها و **تولید محتوای تحلیلی** (مانند تحلیل هوشمند انیمه یا ترتیب تماشا) است.
      * **نحوه دسترسی:** از طریق `gemini-proxy.js` و همچنین مستقیماً در تابع `get-anime-details.js` برای ترجمه داده‌های دریافتی از Jikan.
      * **منطق کلیدی:** تابع `get-anime-details.js` به صورت هوشمند تلاش می‌کند تا در صورت عدم وجود ترجمه، با ارسال پرامپت‌های JSON-Only به Gemini، ترجمه **فارسی روان** را دریافت کند و در صورت بروز خطای Rate Limit، تنها عنوان را ترجمه کند.

3.  **Image Proxy Function (`image-proxy.js`):**

      * برای دور زدن محدودیت‌های CORS و کش کردن تصاویر، این تابع درخواست‌ها را به سمت سرورهای اصلی (مانند `cdn.myanimelist.net` و `img.youtube.com`) فوروارد می‌کند و هدرهای کش مناسب را اضافه می‌نماید.

-----

### 🗺️ ساختار صفحات و قابلیت‌ها

پروژه شامل دو بخش اصلی است که توسط مسیریابی سمت کلاینت (Client-Side Routing) مدیریت می‌شوند:

#### ۱. صفحه اصلی (Home/Search)

  * **جستجوی هوشمند:** امکان جستجوی مستقیم بر اساس عنوان انگلیسی یا فارسی.
  * **ترجمه هوشمند در جستجو:** اگر کاربر عبارت فارسی وارد کند، سیستم ابتدا آن را با Gemini به انگلیسی ترجمه کرده و سپس جستجو را انجام می‌دهد.
  * **پیشنهاد مفهومی (Gemini Powered):** دکمه‌ای برای دریافت لیستی از ۸ انیمه بر اساس توصیف متنی کاربر به زبان فارسی.
  * **نوار منو:** دسترسی سریع به صفحات مختلف:
      * **جستجو (`/`)**
      * **در حال پخش (`/airing`):** نمایش انیمه‌های فصل جاری با قابلیت پیمایش بین فصول (با استفاده از Jikan Seasons API).
      * **برترین‌ها (`/top`):** نمایش لیست‌های رتبه‌بندی شده Jikan.

#### ۲. صفحه جزئیات انیمه (`/anime/:id`)

این صفحه برای هر انیمه به صورت پویا بارگذاری می‌شود و غنی‌ترین محتوا را ارائه می‌دهد:

  * **نمایش جزئیات اولیه:** شامل امتیاز، رتبه، نوع، استودیو و تاریخ پخش.
  * **خلاصه داستان:** نمایش خلاصه ترجمه شده فارسی (در صورت موفقیت API).
  * **دکمه‌های هوشمند AI:**
      * **تحلیل هوشمند:** ارسال خلاصه و ژانرها به Gemini برای دریافت یک تحلیل کامل (نقاط قوت/ضعف) در قالب JSON.
      * **یافتن مشابه:** استفاده از Gemini برای یافتن انیمه‌های مشابه به همراه **دلیل شباهت به زبان فارسی**.
      * **ترجمه مجدد اجباری (`Force Refresh`):** ارسال درخواست مجدد به Gemini برای به‌روزرسانی ترجمه در صورت تغییر مدل.
  * **بخش‌های تب‌بندی شده:**
      * **شخصیت‌ها:** لیست شخصیت‌ها همراه با دکمه **"چت"** برای شروع مکالمه متنی در نقش آن شخصیت با Gemini.
      * **موسیقی‌ها:** لیست تیتراژهای آغازین و پایانی همراه با دکمه جستجوی مستقیم در **YouTube** برای آن آهنگ.
      * **مرتبط:** نمایش انیمه‌های مرتبط و توصیه‌های کاربران.
      * **زیرنویس:** لینک‌های کمکی یا هدایت به کانال تلگرام برای زیرنویس‌های فارسی.

-----

### ⚙️ تنظیمات و متغیرهای محیطی (Environment Variables)

برای اجرای موفقیت‌آمیز توابع Netlify، متغیرهای محیطی زیر ضروری هستند:

| متغیر | توضیحات | استفاده شده در |
| :--- | :--- | :--- |
| **`GEMINI_API_KEY`** | کلید API اختصاصی شما برای دسترسی به Google Gemini. | `gemini-proxy.js`, `get-anime-details.js` |
| **`UPSTASH_REDIS_REST_URL`** | آدرس سرویس REST کامل Upstash Redis. | `get-anime-details.js` |
| **`UPSTASH_REDIS_REST_TOKEN`** | توکن دسترسی به Upstash Redis. | `get-anime-details.js` |

-----

### 🚀 تنظیمات Netlify

فایل `netlify.toml` تضمین می‌کند که تمام مسیرهای وب‌سایت (به جز مسیرهای API) به فایل اصلی `index.html` هدایت شوند تا مسیریابی سمت کلاینت (Client-Side Routing) فعال باشد:

```toml
[build]
  functions = "netlify/functions/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

-----

**با تشکر از توجه شما.**
